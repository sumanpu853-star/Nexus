{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Architecture-first tooling for the Nexus repository.",
  "bin": {
    "nexus": "./src/interfaces/cli.js"
  },
  "scripts": {
    "review:architecture": "node src/interfaces/cli.js",
    "validate:config": "node src/interfaces/cli.js --validate-config",
    "print:config-schema": "node src/interfaces/cli.js --print-config-schema",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "engines": {
    "node": ">=20"
  }
}
